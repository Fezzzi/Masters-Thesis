import { assert } from 'chai'

// todo: Remove this test when some actual mocha test is implemented
describe('Empty dummy test', () => {
  it('should pass an empty test', () => {
    assert.isTrue(true, 'True is expected to be true')
  })
})
